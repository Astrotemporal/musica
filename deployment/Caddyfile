# Navidrome — local + ZeroTier (primary gateway)
:4533 {
    reverse_proxy navidrome:4533
}

# MusicGrabber — local + ZeroTier (download UI)
:7171 {
    reverse_proxy musicgrabber:8080
}

# Public HTTPS (music.plai.do) — requires DNS + router port forward
music.plai.do {
    reverse_proxy navidrome:4533
}
